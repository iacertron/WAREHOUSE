export const validateMaestro = (rows) => {
  if (!rows?.length) return 'El archivo está vacío.';
  return null;
};

export const validateLx02 = (rows) => {
  if (!rows?.length) return 'El archivo está vacío.';
  return null;
};
